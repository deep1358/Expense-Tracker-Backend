{
	"dependencies": {
		"concurrently": "^7.3.0",
		"cors": "^2.8.5",
		"dotenv": "^16.0.1",
		"express": "^4.18.1",
		"express-session": "^1.17.3",
		"mongoose": "^6.3.5",
		"passport": "^0.6.0",
		"passport-google-oauth20": "^2.0.0"
	},
	"devDependencies": {
		"nodemon": "^2.0.16"
	},
	"scripts": {
		"start": "node index.js",
		"dev": "nodemon index --ignore frontend/",
		"populate": "node populate/index.js",
		"frontend": "npm start --prefix frontend",
		"concurrent": "concurrently \"npm run dev\" \"npm run frontend\"",
		"frontend-install": "npm install --prefix frontend"
	}
}
